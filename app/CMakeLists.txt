# include_directories(${CMAKE_SOURCE_DIR}/include)
# link_libraries(codility)
# add_executable(MyProject main.cpp)

include_directories(${CMAKE_SOURCE_DIR}/include)
link_libraries(codility)
add_executable(MyProject main.cpp)
